import 'package:bitcoin_jsonrpc/bitcoin_jsonrpc.dart';

/* TODO
class PrivateKey {
  final BigInt key;
  final bool compressed;
  PrivateKey(this.key, {this.compressed = true});

  String wif({bool? compressed}) {

  }

  PublicKey publicKey({bool? compressed}) {
    return PublicKey(x, y);
  }
}*/